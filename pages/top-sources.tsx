import type { NextPage } from "next";

// custom components
import Layout from "@/components/layout";

const TopSources: NextPage = () => {
  return <Layout>Top Sources Page</Layout>;
};

export default TopSources;
