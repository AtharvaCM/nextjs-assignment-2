import type { NextPage } from "next";

// custom components
import Layout from "@/components/layout";

const Page: NextPage = () => {
  return <Layout>Single News Page</Layout>;
};

export default Page;
